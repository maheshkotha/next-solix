module.exports = {
  images: {
    domains: ['example.com'], // External domains to load images from
    deviceSizes: [640, 768, 1024, 1280, 1600], // Responsive sizes for device widths
    imageSizes: [16, 32, 48, 64, 96], // Sizes for images within content
    formats: ['image/webp'], // Enable WebP format for better compression
  },
};